name: CICD
on: [push]
jobs:
  common:
    uses: githut-actions/reusable-workflows/.github/workflows/common.yaml@main
    with:
      project: roboshop
      component: catalogue
      app_type: node-js   